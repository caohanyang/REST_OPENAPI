{
  "basePath": "/v1/media/{media-id}",
  "paths": {
    "/comments/{comment-id}": {
      "DELETE": {
        "parameters": [
          {
            "in": "query",
            "name": "access_token",
            "description": "A valid access token.",
            "type": "integer",
            "required": true
          }
        ]
      }
    },
    "/likes": {
      "DELETE": {
        "parameters": [
          {
            "in": "query",
            "name": "access_token",
            "description": "A valid access token.",
            "type": "integer",
            "required": true
          }
        ]
      }
    }
  },
  "produces": "application/json",
  "host": "api.instagram.com",
  "schemes": [
    "https"
  ],
  "swagger": "2.0",
  "info": "This is the information of the API"
}